// use unit14::kinds::PrimaryColor;
// use unit14::utils::mix;
use unit14::mix;
use unit14::PrimaryColor;

fn main() {
    let red = PrimaryColor::Red;
    let yellow = PrimaryColor::Yellow;
    mix(red, yellow);
}
